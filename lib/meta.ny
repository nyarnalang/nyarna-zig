\library()

\declare:
  params = \keyword:
    callable: \Ast
    exclude : \List(\Ast) {varargs}
  \end(keyword)

  access = \keyword:
    subject: \Ast
    name   : \Ast
  \end(keyword)
\end(declare)