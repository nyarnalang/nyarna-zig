=== if-Statement
--- input
\if(true):
  foo
\end(if)
\if(false):
  bar
:else:
  baz
\end(if)
--- ast
+ROOT "standalone"
  >CONTENT
  +CALL
    +TARGET
      +FUNC
        =NAME "concatOf"
      -FUNC
    -TARGET
    +PROTO "items"
      +VARARGS
        +CALL
          +TARGET
            +FUNC
              =NAME "if"
            -FUNC
          -TARGET
          +PROTO "condition"
            =ENUM Boolean "true"
          -PROTO
          +PROTO "then"
            =RAW Raw "foo"
          -PROTO
          +PROTO "else"
            =NONE
          -PROTO
        -CALL
        =RAW Space "\n"
        +CALL
          +TARGET
            +FUNC
              =NAME "if"
            -FUNC
          -TARGET
          +PROTO "condition"
            =ENUM Boolean "false"
          -PROTO
          +PROTO "then"
            =RAW Raw "bar"
          -PROTO
          +PROTO "else"
            =RAW Raw "baz"
          -PROTO
        -CALL
      -VARARGS
    -PROTO
  -CALL
-ROOT
--- document
=RAW Raw "foo\nbaz"