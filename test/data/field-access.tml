=== Field access
--- input
\declare:
  Data = \Record:
    a, b : \Raw;
  \end(Record)
\end(declare)

\declare:
  foo = \func:
    data : \Data;
  :body:
    (\data::a, \data::b)
  \end(func)
\end(declare)

\foo(\Data(x, y))
--- rawast
+PARA
  +RCALL
    =FUNCREF .std.intrinsic.declare
    >ARG namespace
    =VOID
    >ARG public
    +AST
      +DEF Data
        +RCALL
          =PROTO record
          >ARG fields
          +LOC a
            =TYPE raw
          -LOC
          +LOC b
            =TYPE raw
          -LOC
        -RCALL
      -DEF
    -AST
    >ARG private
    +AST
      =VOID
    -AST
  -RCALL
-PARA
+PARA
  +RCALL
    =FUNCREF .std.intrinsic.declare
    >ARG namespace
    =VOID
    >ARG public
    +AST
      +DEF foo
        +RCALL
          =FUNCREF .std.intrinsic.func
          >ARG return
          +AST
            =VOID
          -AST
          >ARG params
          +AST
            +LOC data
              +TYPE
                =SYMREF [0]Data
              -TYPE
            -LOC
          -AST
          >ARG body
          +AST
            =LIT text "("
            +ACCESS
              +SUBJECT
                =SYMREF [0]data
              -SUBJECT
              =ID "a"
            -ACCESS
            =LIT text ", "
            +ACCESS
              +SUBJECT
                =SYMREF [0]data
              -SUBJECT
              =ID "b"
            -ACCESS
            =LIT text ")"
          -AST
        -RCALL
      -DEF
    -AST
    >ARG private
    +AST
      =VOID
    -AST
  -RCALL
-PARA
+PARA
  +UCALL
    +TARGET
      =SYMREF [0]foo
    -TARGET
    +PROTO pos
      +UCALL
        +TARGET
          =SYMREF [0]Data
        -TARGET
        +PROTO pos
          =LIT text "x"
        -PROTO
        +PROTO pos
          =LIT text "y"
        -PROTO
      -UCALL
    -PROTO
  -UCALL
-PARA
--- expr
+CALL
  =FUNCREF .doc.document.foo
  >ARG data
  +CALL
    =TYPE record .doc.document.Data
    >ARG a
    =TEXT raw "x"
    >ARG b
    =TEXT raw "y"
  -CALL
-CALL
--- document
=RAW Raw "(x, y)"