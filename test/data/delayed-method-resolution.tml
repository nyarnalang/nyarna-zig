=== Delayed method resolution
--- input
\declare:
  foo = \record:
    a, b: \Raw
  \end(record)
\end(declare)

\declare(\foo):
  double = \func:
  :body:
    \this::concat()\this::concat()
  \end(func)
  concat = \func:
  :body:
    \this::a\this::b
  \end(func)
\end(declare)

\foo(foo, bar)::double()
--- ast
+CALL
  +TARGET
    =SYMREF .doc.double
  -TARGET
  +PARAM "this"
    +CALL
      +TARGET
        =SYMREF .doc.foo
      -TARGET
      +PARAM "a"
        =LIT TEXT "foo"
      -PARAM
      +PARAM "b"
        =LIT TEXT "bar"
      -PARAM
    -CALL
  -PARAM
-CALL
--- document
=RAW Raw "foobarfoobar"