=== Direct Recursion
--- input
\declare:
  foo = \func:
    recurse       : \Bool
    before, after : \Raw
  :body:
    \before\if(\recurse, \foo(false, two, three))\after
  \end(func)
\end(declare)

\foo(true, one, four)
\foo(false, one, two)
--- expr
+CALL
  =FUNCREF .doc.input.foo
  >ARG recurse
  =ENUM Bool "true"
  >ARG before
  =TEXT Raw "one"
  >ARG after
  =TEXT Raw "four"
-CALL
=TEXT Raw "\n"
+CALL
  =FUNCREF .doc.input.foo
  >ARG recurse
  =ENUM Bool "false"
  >ARG before
  =TEXT Raw "one"
  >ARG after
  =TEXT Raw "two"
-CALL
--- document
=TEXT Raw "onetwothreefour\nonetwo"