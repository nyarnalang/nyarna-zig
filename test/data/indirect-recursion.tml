=== Indirect Recursion
--- input
\declare:
  foo = \func:
    recurse: \Boolean;
  :body:
    foo\if(\recurse, \bar(false))
  \end(func)

  bar = \func:
    recurse: \Boolean;
  :body:
    bar\if(\recurse, \foo(false))
  \end(func)
\end(declare)

\foo(true)
\bar(true)
--- ast
+ROOT "standalone"
  >CONTENT
  +CALL
    +TARGET
      +FUNC
        =NAME "concatOf"
      -FUNC
    -TARGET
    +PROTO "items"
      +VARARGS
        =RAW Raw "foobar"
        =RAW Space "\n"
        =RAW Raw "barfoo"
      -VARARGS
    -PROTO
  -CALL
-ROOT
--- expr
+CALL
  =FUNCREF .doc.document.foo
  >ARG recurse
  =ENUM Boolean "true"
-CALL
=TEXT literal "\n"
+CALL
  =FUNCREF .doc.document.bar
  >ARG recurse
  =ENUM Boolean "true"
-CALL